- Run ADRS:
	- API Problem: Deepseek V4 Pro's `reasoning_effort` defaults to `high`, while GPT-5's `reasoning_effort` defaults to `medium`. That's why Deepseek is significantly slower than GPT-5 (in the ADRS report).
	- Action: Set Deepseek V4 Pro's `reasoning_effort` to `medium`.
	- Other:
		- `checkpoint_interval` = 1
		- Log Level = DEBUG
		- Add code to `openai.py` to compute API speed.
	- Insight:
		- prism needs scipy -> should we install it or not?
		