# Open a New Problem Framing (problem_view)

The search has explored the framings listed below. Propose a GENUINELY NEW way to
FRAME this problem — a new problem_view — that is NOT present in the map. You may
SIMPLIFY aggressively: a clean, fundamentally different model is far better than
piling features onto the current best.

# Explored Map (framings already tried — do NOT re-implement any of these)
{explored_map}

# Current Best (jump-off point ONLY — do not merely refine it)
{seed}

# Task
{task_objective}
Provide a COMPLETE new program built on a fundamentally different framing.

```{language}
# Your new program here
```

**CRITICAL**: Your program must differ in its CORE problem model from everything in
the explored map. Include a concise docstring naming the new framing and stating
why it is fundamentally different.
